<Window x:Class="eZet.Eve.EveProfiteer.Views.AnalyzerProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analyzing..." Height="200" Width="300">
    <Grid Margin="30">
        <StackPanel>
        <TextBlock Text="{Binding Status, Mode=OneWay}"></TextBlock>
        <ProgressBar Name="AnalyzerProgressBar" IsIndeterminate="True" Height="20"></ProgressBar>
        </StackPanel>
    </Grid>
</Window>
